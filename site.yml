---
- name: Install common packages
  hosts: all
  tags:
    - base
    - packages
    - setup
    - base_packages
    - base-packages
    - update
    - upgrade
  roles:
    - base_packages

- name: Setup SSH and manage user
  hosts: all
  tags:
    - ssh
    - sshd
    - ssh-auth
    - ssh_auth
    - ssh-keys
    - update
    - users
    - user
    - default
  roles:
    - ssh-auth
    - sshd

- name: Create good dotfiles
  hosts: all
  tags:
    - dotfiles
    - bashrc
    - vimrc
    - home
    - fancy
    - bat
  roles:
   - dotfiles
   - bat

- name: install and configure weechat
  hosts: all
  tags:
    - defualt
    - weechat
    - install
    - conf
    - configure
  roles:
   - weechat
   - weechat-matrix-setup
